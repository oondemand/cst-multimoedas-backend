apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-tls
  namespace: cst-multimoedas
spec:
  secretName: app-tls
  issuerRef:
    name: letsencrypt-nginx
    kind: ClusterIssuer
  dnsNames:
    - api.cst-multimoedas.oondemand.online
    - cst-multimoedas.oondemand.online
