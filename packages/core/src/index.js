const { createCoreApp } = require("../../central-oon-core-backend/src/app");
const { startCoreServer } = require("./bootstrap/startServer");

module.exports = {
  createCoreApp,
  startCoreServer,
};
