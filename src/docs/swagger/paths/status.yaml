/:
  get:
    tags:
      - Status
    description: Retorna o status atual do serviço e da conexão com o banco de dados
    responses:
      200:
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example: "cst-multimoedas-backend rodando - vs 1.0.0"
                database:
                  type: string
                  example: "Conectado em cst-multimoedas"
